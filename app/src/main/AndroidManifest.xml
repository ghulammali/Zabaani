<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zabaani">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FillBlanks"></activity>
        <activity android:name=".LevelActivity" />
        <activity android:name=".MCQ" />
        <activity android:name=".ScoreView" />
        <activity android:name=".McqTutorialActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BlankTutorialActivity" />
        <activity android:name=".WorldLevelsActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".OptionsActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>